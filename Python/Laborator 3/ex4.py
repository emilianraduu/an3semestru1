def getString(item, index):
    for 
def build_xml_element(tag, content, *keys):
    print(keys)
    print([i for i in keys])
    print('%s ' * len(keys) % lambda (getString(index, item in enumerate())))

build_xml_element('a', 'b', 'href="http://python.org"','_class="my-link"', 'id="someid"')